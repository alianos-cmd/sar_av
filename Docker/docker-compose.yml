version: "3.9"

services:
  ros2:
    build: .
    container_name: ros2_robot
    network_mode: host
    privileged: true
    volumes:
      - /dev:/dev
      - ./ros_ws:/ros_ws
    stdin_open: true
    tty: true

